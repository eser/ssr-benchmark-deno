{
  "lock": false,
  "tasks": {
    "build": "deno task --cwd ./pkg/solid-js build && deno task --cwd ./pkg/preact build",
    "start": "deno task build && deno run -A --v8-flags=--max-old-space-size=8192 main.ts"
  }
}
